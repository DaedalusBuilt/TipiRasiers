/**
 * scripts/migrate.js — No longer needed (data is in PostgreSQL/Supabase).
 * Kept as a placeholder so npm run migrate doesn't crash.
 */
console.log('No migration needed — database is managed by Supabase/PostgreSQL.');
process.exit(0);
