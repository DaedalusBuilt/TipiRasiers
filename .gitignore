# Dependencies
node_modules/

# Environment variables (never commit secrets)
.env

# macOS
.DS_Store

# Local uploads (use Supabase Storage in production)
public/images/uploads/*
!public/images/uploads/.gitkeep

# Old data files (replaced by PostgreSQL)
data/*.json
data/*.db
data/*.db-shm
data/*.db-wal
